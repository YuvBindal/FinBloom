import { Transaction, TransactionMetadata } from '../transactions';
import { BaseTransaction } from '../transactions/common';
import { PseudoTransaction } from '../transactions/transaction';
import { BaseRequest, BaseResponse } from './baseMethod';
export interface TxRequest extends BaseRequest {
    command: 'tx';
    transaction?: string;
    ctid?: string;
    binary?: boolean;
    min_ledger?: number;
    max_ledger?: number;
}
export interface TxResponse<T extends BaseTransaction = Transaction | PseudoTransaction> extends BaseResponse {
    result: {
        hash: string;
        ctid?: string;
        ledger_index?: number;
        meta?: TransactionMetadata | string;
        validated?: boolean;
        date?: number;
    } & T;
    searched_all?: boolean;
}
//# sourceMappingURL=tx.d.ts.map