import type { Client, SubmitResponse, Wallet } from '..';
import { TxResponse } from '../models/methods';
import { Transaction } from '../models/transactions';
declare function submit(this: Client, transaction: Transaction | string, opts?: {
    autofill?: boolean;
    failHard?: boolean;
    wallet?: Wallet;
}): Promise<SubmitResponse>;
declare function submitAndWait<T extends Transaction = Transaction>(this: Client, transaction: T | string, opts?: {
    autofill?: boolean;
    failHard?: boolean;
    wallet?: Wallet;
}): Promise<TxResponse<T>>;
export { submit, submitAndWait };
//# sourceMappingURL=submit.d.ts.map